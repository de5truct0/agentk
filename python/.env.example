# AGENT-K Council Mode Configuration
# ===================================
# Copy this file to .env and add your API keys to enable council mode.
#
# By default, AGENT-K uses Claude Code CLI which requires no API keys.
# Council mode enables multi-LLM consensus using GPT, Gemini, and Claude APIs.

# OpenAI API Key (for GPT-4o)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Google Gemini API Key (for Gemini 2.0)
# Get your key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Anthropic API Key (optional - Claude Code CLI is preferred)
# Only needed if you want to use the API instead of CLI
# Get your key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Usage:
# ------
# 1. Copy this file: cp .env.example .env
# 2. Add your API keys above
# 3. Run agentk8 and use /council or /solo commands
#
# Modes:
# - Default (no .env): Uses Claude Code CLI only
# - /solo: Uses multiple Claude instances with different personas
# - /council: Uses GPT + Gemini + Claude for multi-LLM consensus
